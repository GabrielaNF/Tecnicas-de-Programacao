package P1;

public class QuestoesTeoricas {
    /*
     * 1- Encapsulamento eh um metodo de ocultar os elementos de uma classe (usa a palavra "private"), deixando como private para que o usuario nao tenha acesso.
     * E deixando publico somente a parte necessaria ao usuario (getters e setters e outros metodos)
     * 
     * 2- A funcao do metodo construtor eh colocar os valores iniciais dos atributos quando instancia
     * 
     * 3- O objeto nao inicializa, logo, nao funciona.
     * 
     * 4- A classe Object eh pai de todas as classes no Java.
     * 
     * 5- Sim, porem tem que ser de tipos diferentes.
     * Exemplo, int valor; String valor[], sao de tipos diferentes, logo podem ser utilizadas, pois o Java reconhece o tipo
     *  que esta sendo utilizado.
     * Tambem se tiverem mesmo nome mas nao estiverem como variaveis globais, funciona pois so vao ser lidas dentro daquela
     * parte do programa.
     */
}
